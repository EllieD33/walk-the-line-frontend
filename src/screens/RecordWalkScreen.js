import { SafeAreaView, Text } from "react-native"

const RecordWalkScreen = () => {
    return (
        <SafeAreaView>
            <Text>RECORD WALK SCREEN</Text>
        </SafeAreaView>
    )
};

export default RecordWalkScreen;