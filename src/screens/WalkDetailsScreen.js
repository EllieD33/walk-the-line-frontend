import { SafeAreaView, Text } from "react-native"

const ProfileScreen = () => {
    return (
        <SafeAreaView>
            <Text>WALK DETAILS SCREEN</Text>
        </SafeAreaView>
    )
};

export default ProfileScreen;